services:
  backend:
    build: ./backend
    image: bacon_distance_backend
    ports:
      - "5000:5000"
  frontend:
    build: ./frontend
    image: bacon_distance_frontend
    ports:
      - "5173:5173"
